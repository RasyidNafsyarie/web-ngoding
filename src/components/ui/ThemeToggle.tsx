"use client";

/**
 * Neo-brutalism menggunakan single definitive light-mode palette (DESIGN.md).
 * Dark mode tidak ada dalam design system ini.
 *
 * Komponen ini di-export sebagai null agar tidak ada breaking import.
 * File tetap ada untuk kompatibilitas jika dark mode ditambahkan di masa depan.
 */
export function ThemeToggle() {
  return null;
}
